{
  "name": "frontend",
  "version": "0.1.0",
  "description": "Frontend for High Score Game",
  "scripts": {
    "start": "echo 'Use dfx start to run the local replica'",
    "build": "echo 'No build step for plain HTML/JS'"
  },
  "dependencies": {
    "@dfinity/agent": "^0.19.2",
    "@dfinity/candid": "^0.19.2",
    "@dfinity/principal": "^0.19.2"
  },
  "devDependencies": {},
  "engines": {
    "node": "^16 || ^18"
  }
}